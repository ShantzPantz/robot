services:
  web:
    build: .
    container_name: fastapi-server
    restart: unless-stopped
    ports:
      - "8000:8000"        
    volumes:
      - "/tmp/uploads:/app/uploads"
    environment: 
      - LOG_LEVEL=INFO

volumes:
  uploads:

      