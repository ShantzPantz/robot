services:
  web:
    build: .
    container_name: fastapi-server
    restart: unless-stopped
    ports:
      - "8000:8000"        
    volumes:
      - "/tmp/audio:/app/audio"
    environment: 
      - LOG_LEVEL=INFO

volumes:
  uploads:

      